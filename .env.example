# Copy to .env and fill in your values before starting the server.

# Port the Express server listens on (default: 3000)
PORT=3000

# Publicly reachable base URL — this is what goes into the GoHighLevel template URL.
# Must be accessible from the internet so GHL can fetch the image at send-time.
SERVER_BASE_URL=https://your-server.example.com
